{
  "name": "e2e-testing",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "cypress": "^12.14.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "merge": "mochawesome-merge cypress/reports/*.json -o cypress/reports/report.json",
    "generate": "marge --inline cypress/reports/report.json -o cypress/reports/"
  }
}
